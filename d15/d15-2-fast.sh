#!/bin/bash

for ((i=0; ; i+= 85)); do
  [[ $(((i+1+10)%13)) -eq 0 ]] &&
  [[ $(((i+3+17)%19)) -eq 0 ]] &&
  [[ $(((i+4+1)%7)) -eq 0 ]] &&
  [[ $(((i+6+1)%3)) -eq 0 ]] &&
  [[ $(((i+7+0)%11)) -eq 0 ]] &&
    echo $i && exit
done
